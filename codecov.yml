coverage:
  status:
    project:
      default:
        target: 35%
        threshold: 1%
        if_ci_failed: error
    patch:
      default:
        target: auto
        threshold: 1%
        if_ci_failed: error

comment: false
